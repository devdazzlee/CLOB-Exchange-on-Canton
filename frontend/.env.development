# =============================================================================
# Development Environment Variables
# =============================================================================
# These are used by Vite dev server (npm run dev).
# Override any value here for local development.
# =============================================================================

# Backend API (local dev server)
VITE_API_BASE_URL=http://localhost:3001/api

# Backend root URL (WebSocket is derived from this when VITE_WS_URL is unset)
VITE_BACKEND_URL=http://localhost:3001

# Explicit WebSocket URL (overrides VITE_BACKEND_URL derivation)
# VITE_WS_URL=ws://localhost:3001/ws
