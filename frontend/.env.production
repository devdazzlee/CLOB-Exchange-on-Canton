# =============================================================================
# Production Environment Variables
# =============================================================================
# Baked into the frontend bundle at BUILD TIME by Vite.
# After changing any value you MUST rebuild: npm run build
# =============================================================================

# Backend API base URL
VITE_API_BASE_URL=https://clob-exchange-on-canton-sg12.vercel.app/api

# Backend root URL (used to derive WebSocket URL)
VITE_BACKEND_URL=https://clob-exchange-on-canton-sg12.vercel.app

# WebSocket URL — NOTE: Vercel serverless does NOT support WebSockets.
# If you move the backend to a VPS/VM/container that supports WS, uncomment:
# VITE_WS_URL=wss://clob-exchange-on-canton-sg12.vercel.app/ws
